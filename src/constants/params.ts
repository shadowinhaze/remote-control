export enum ScreenShotSize {
  height = 100,
  width = 100,
}

export enum ReservePort {
  wssPort = 8080,
  httpPort = 3000,
}

export enum MouseDelayForDrawingFigure {
  rectangle = 200,
  square = 200,
}
